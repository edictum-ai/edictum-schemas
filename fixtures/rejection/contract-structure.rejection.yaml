suite: contract-structure
version: 1
description: "Rules with structural errors: missing or invalid fields within individual rules"

fixtures:
  - id: "rej-010"
    description: "Contract missing id field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "id"

  - id: "rej-011"
    description: "Contract id is empty string"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: ""
          type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "id"

  - id: "rej-012"
    description: "Contract id with uppercase characters"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: "UPPER-CASE"
          type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "id"

  - id: "rej-013"
    description: "Contract id with dots — only lowercase, digits, hyphens, underscores allowed"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: "has.dot"
          type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "id"

  - id: "rej-014"
    description: "Contract missing type field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "type"

  - id: "rej-015"
    description: "Contract with invalid type value"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: rule
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "type"

  - id: "rej-016"
    description: "Pre rule missing tool field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: pre
          when:
            args.x: { equals: 1 }
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "tool"

  - id: "rej-017"
    description: "Pre rule missing when field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: pre
          tool: "*"
          then:
            action: block
            message: "denied"
    expected:
      rejected: true
      error_contains: "when"

  - id: "rej-018"
    description: "Pre rule missing then field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
    expected:
      rejected: true
      error_contains: "then"

  - id: "rej-019"
    description: "Pre rule then block missing effect"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            message: "denied"
    expected:
      rejected: true
      error_contains: "action"

  - id: "rej-020"
    description: "Pre rule then block missing message"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: pre
          tool: "*"
          when:
            args.x: { equals: 1 }
          then:
            action: block
    expected:
      rejected: true
      error_contains: "message"

  - id: "rej-021"
    description: "Post rule missing tool field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: post
          when:
            result.status: { equals: "error" }
          then:
            action: warn
            message: "error detected"
    expected:
      rejected: true
      error_contains: "tool"

  - id: "rej-022"
    description: "Post rule missing when field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: post
          tool: "*"
          then:
            action: warn
            message: "warning"
    expected:
      rejected: true
      error_contains: "when"

  - id: "rej-023"
    description: "Post rule missing then field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: post
          tool: "*"
          when:
            result.status: { equals: "error" }
    expected:
      rejected: true
      error_contains: "then"

  - id: "rej-024"
    description: "Session rule missing limits field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: session
          then:
            action: block
            message: "limit exceeded"
    expected:
      rejected: true
      error_contains: "limits"

  - id: "rej-025"
    description: "Session rule with empty limits object"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: session
          limits: {}
          then:
            action: block
            message: "limit exceeded"
    expected:
      rejected: true
      error_contains: "limits"

  - id: "rej-026"
    description: "Session rule missing then field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: session
          limits:
            max_tool_calls: 10
    expected:
      rejected: true
      error_contains: "then"

  - id: "rej-027"
    description: "Session rule then block missing message"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: session
          limits:
            max_tool_calls: 10
          then:
            action: block
    expected:
      rejected: true
      error_contains: "message"

  - id: "rej-028"
    description: "Sandbox rule missing both tool and tools fields"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: sandbox
          within:
            - "/safe"
          message: "denied"
    expected:
      rejected: true
      error_contains: "tool"

  - id: "rej-029"
    description: "Sandbox rule missing both within and allows fields"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: sandbox
          tool: "Bash"
          message: "denied"
    expected:
      rejected: true
      error_contains: "within"

  - id: "rej-030"
    description: "Sandbox rule missing message field"
    bundle:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: c1
          type: sandbox
          tool: "Bash"
          within:
            - "/safe"
    expected:
      rejected: true
      error_contains: "message"
