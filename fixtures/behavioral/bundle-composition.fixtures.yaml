suite: bundle-composition
version: 1
description: "Multiple rules in a single Ruleset: ordering, interaction, and mixed types"

fixtures:
  - id: "bundle-001"
    description: "First matching block rule wins over later allow"

    contract:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: deny-rm
          type: pre
          tool: "Bash"
          when:
            args.command:
              contains: "rm -rf"
          then:
            action: block
            message: "rm -rf is blocked"
        - id: allow-bash
          type: pre
          tool: "Bash"
          when:
            args.command:
              exists: true
          then:
            action: ask
            message: "Bash commands are generally allowed"

    envelope:
      tool_name: "Bash"
      arguments:
        command: "rm -rf /data"

    expected:
      verdict: denied
      message_contains: "rm -rf"

  - id: "bundle-002"
    description: "Non-matching block falls through to allow rule"

    contract:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: deny-rm
          type: pre
          tool: "Bash"
          when:
            args.command:
              contains: "rm -rf"
          then:
            action: block
            message: "rm -rf is blocked"
        - id: allow-bash
          type: pre
          tool: "Bash"
          when:
            args.command:
              exists: true
          then:
            action: ask
            message: "Bash commands are generally allowed"

    envelope:
      tool_name: "Bash"
      arguments:
        command: "ls -la"

    expected:
      verdict: allowed

  - id: "bundle-003"
    description: "Mixed pre and post rules in one Ruleset"

    contract:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: pre-check
          type: pre
          tool: "Bash"
          when:
            args.command:
              contains: "rm"
          then:
            action: block
            message: "rm is blocked"
        - id: post-check
          type: post
          tool: "Bash"
          when:
            result.output:
              contains: "error"
          then:
            action: warn
            message: "Command produced errors"

    envelope:
      tool_name: "Bash"
      arguments:
        command: "cat file.txt"

    result:
      output: "error: file not found"

    expected:
      verdict: warned
      message_contains: "errors"

  - id: "bundle-004"
    description: "Mixed pre + session rules: session limit blocks even when pre allows"

    contract:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: allow-reads
          type: pre
          tool: "Read"
          when:
            args.file_path:
              exists: true
          then:
            action: ask
            message: "Read operations approved"
        - id: rate-limit
          type: session
          limits:
            max_tool_calls: 5
          then:
            action: block
            message: "Session tool call limit reached"

    session:
      tool_calls: 6

    envelope:
      tool_name: "Read"
      arguments:
        file_path: "/tmp/data.txt"

    expected:
      verdict: denied
      message_contains: "limit"

  - id: "bundle-005"
    description: "Mixed pre + sandbox rules: sandbox restricts paths"

    contract:
      apiVersion: edictum/v1
      kind: Ruleset
      metadata:
        name: test-bundle
      defaults:
        mode: enforce
      rules:
        - id: allow-write
          type: pre
          tool: "Write"
          when:
            args.content:
              exists: true
          then:
            action: ask
            message: "Write with content approved"
        - id: sandbox-write
          type: sandbox
          tool: "Write"
          within:
            - "/home/user/project"
          message: "Write restricted to project dir"

    envelope:
      tool_name: "Write"
      arguments:
        file_path: "/etc/shadow"
        content: "malicious"

    expected:
      verdict: denied
      message_contains: "project dir"
